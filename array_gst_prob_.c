#include <stdio.h>

int main (){
    float price[3];

    printf("GIVE THE THE PRICE OF 3 ITEMS \n");
    scanf("%f",&price[0]);
     scanf("%f",&price[1]);
      scanf("%f",&price[2]);

      printf("the price of first item is %.2f with gst %.2f\n",price[0],price[0]*(0.18));
      printf("the price of first item is %.2f with gst %.2f\n",price[1],price[1]*(0.18));
      printf("the price of first item is %.2f with gst %.2f\n",price[2],price[2]*(0.18));
      
    
    return 0;
}

     //////////questioj is take price and print it with gst


            /////////////////////////////////////good done by yourself ///////////////////////////////////////////////